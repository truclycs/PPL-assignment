
        int main() 
        {
            int lower, upper, count;
            lower = 5; upper = 7; count = 1;  
            // Use current time as 
            // seed for random generator 
            srand(time(0));
            printRandoms(lower, upper, count);  
            return 0;
        }
        