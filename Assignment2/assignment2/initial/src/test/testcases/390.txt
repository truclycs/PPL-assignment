
		int main()
		{
    		int n, count;
			count = 0;
    		printf("Enter an integer: ");
    		scanf("%d", n);
			do {
        		n = n / 10;
        		count = count + 1;
    		}
    		while (n != 0);
    
    		printf("Number of digits: %d", count);
			return 0;
		}