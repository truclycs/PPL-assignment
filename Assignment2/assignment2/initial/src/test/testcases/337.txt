boolean __testSomeBlocks() {
		{ a * b = b * c = c * d; }
		{ 
			str = "There's always only one truth!";
			printf(str);
			toJapanese(str);
			{
				return str == "Shinjitsu wa itsumo hitotsu!";
			}
		}
		}