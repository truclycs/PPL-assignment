void main() {
	if(i>5e2) {
		i=a+3; // Basic add op
		foo(2)[3+x] = a[b[2]] + 3; // Function invocation
		boolean t;
		t = B = T = S = true || (var - 9 % 5 && (.5e7 != sun)); 
		/*A pretty long expression
		that makes no sense*/
	}
} 
        