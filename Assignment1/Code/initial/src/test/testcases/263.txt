int main() {
                    if (h > c) x = min(p, b / 2);
                    else (h * x + c * min(f, (b - x * 2) / 2));
                    cout(h * min(p, (b - x * 2) / 2) + c * x);
                    }
                    return 0;
                }