 
                    int main() {
                        int s, n;
                        cin >> s >> n;
                        pair<int, int> x[n];
                        for (int i = 0; i < n; i++) sort(x, x + n);                    
                        for (int i = 0; i < n; i++) {
                            if (x[i] >= s) {
                                cout << "NO";
                                return 0;
                            }
                        }                    
                        cout << "YES";
                        return 0;
                    }